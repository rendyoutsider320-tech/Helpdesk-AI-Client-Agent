Playbook Engine (template)
================================

This folder contains a minimal Playbook Engine template and an example playbook.

The engine loads YAML playbooks (`playbooks/*.yaml`) and executes defined steps using registered actions.
This is a starting point — implement secure action whitelisting, auditing, and dry-run support before use in production.
